import { apiPager } from '~/server/utils/observed-database'
export default defineEventHandler(async(event)=>{const q=getQuery(event);const source=await dbQuery('SELECT genres FROM subjects WHERE id=$1 OR slug=$1 LIMIT 1',[String(q.subjectId||'')]);if(!source.rows[0])return apiOk({subjectList:[],pager:apiPager(1,20,0)});const genres=source.rows[0].genres||[];const values:any[]=[genres,String(q.subjectId||'')];return apiOk(await listObservedSubjects('s.published=true AND s.genres && $1::text[] AND s.id<>$2 AND s.slug<>$2',values,'s.popularity DESC',Math.max(1,Number(q.page||1)),Math.min(30,Number(q.perPage||12))))})
