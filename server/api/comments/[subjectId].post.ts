export default defineEventHandler(async(event)=>{const user=await requireUser(event);await enforceRateLimit(event,'comment',10,3600);const subjectId=String(getRouterParam(event,'subjectId')),body=await readBody<any>(event),content=String(body.content||'').trim();if(content.length<2||content.length>2000)throw createError({statusCode:400,statusMessage:'Commentaire invalide'});const rating=body.rating?Math.max(1,Math.min(10,Number(body.rating))):null;const result=await dbQuery('INSERT INTO comments(subject_id,user_id,content,rating) VALUES($1,$2,$3,$4) RETURNING *',[subjectId,user.id,content,rating]);return {item:result.rows[0]}})
