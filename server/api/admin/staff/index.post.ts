export default defineEventHandler(async(event)=>{requireAdmin(event);const b=await readBody<any>(event);if(!b.name||!b.slug)throw createError({statusCode:400,statusMessage:'Nom et slug requis'});const result=await dbQuery('INSERT INTO staff(id,slug,name,biography,avatar_url,birth_date,country_name,staff_type) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *',[String(b.id||crypto.randomUUID()),String(b.slug),String(b.name),String(b.biography||''),String(b.avatarUrl||''),b.birthDate||null,String(b.countryName||''),Number(b.staffType||1)]);return {item:result.rows[0]}})
